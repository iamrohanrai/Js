//  String - means sequence of characters, strored in "", '', ``
let name = "rohan";
const name2 = "rai";

// Number - integer = 25, float = 25.2
let age = 25;

// Boolean - true, false
let isPaid = true;

// null - means abhi usmai kuch ni hai
let favouriteClass = null;

/*
null is a primitive value, but due to legacy reasons from the early days of JavaScript, typeof null incorrectly returns "object".
*/

// undefined means actually abhi usage nahi hai but can be used afterwards
let homeTown;

// array - collection of packets
let skills = ["html", "css", "js"];

// objects -
let studentsProfile = {
  name: "Rohan",
  age: 25,
  ispaid: true,
  favouriteClass: null,
};

// console.log(typeof favouriteClass);
